version: "3"
services:
  db:
    build: ./back/src/db/
  backend:
    build: ./back/
    depends_on:
      - db
  nginx:
    build: ./front/
    ports:
      - 80:80
    depends_on:
      - db
      - backend
